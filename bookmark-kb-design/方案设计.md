# 浏览器书签入库知识库 · 方案设计

- 日期：2026-09-05
- 状态：首批铺开完成（`方向` 目录 4 方向 + 顶层 AI/架构），进入人工校准期
- 源数据：`source/bookmarks.html`（浏览器导出，2026-09-05，4097 条；**个人全量书签不入库**，已 gitignore，可随时从浏览器重新导出）

## 1. 背景与目标

浏览器书签积累多年（4000+ 条），大量低价值散链混杂少数高价值文章。目标：

1. **分级筛选**：机器首轮按 A/B/C/D 分级，人工校准后处置；
2. **A 级转写**：高价值文章转写为知识库笔记（ascension 站点 `src/content/docs/`）；
3. **B/C 级归档**：按主题分组保留为导航清单，作为延伸阅读与速查层；
4. **D 级清理**：错放、重复、低质量平台链接从浏览器书签中整批删除。

## 2. 数据画像（2026-09-05 导出）

| 顶层目录 | 数量 | 说明 |
|---|---|---|
| website | 981 | 各类网站 |
| 阅读 | 973 | 阅读材料 |
| 方向 | 576 | 技术方向（本次铺开对象） |
| 技术 | 506 | 技术杂链 |
| TC | 453 | — |
| 工具 | 250 | 工具站 |
| AI | 148 | AI 相关 |
| 架构 | 126 | 架构相关 |
| 其余 | 84 | ai-video/固定/加盟 等 |

URL 去重后 4038 条。`方向` 目录与知识库 17 方向架构对齐，是铺开期优先对象：

| 方向 | 数量 | 状态 |
|---|---|---|
| Java | 215 | 原会话已完成首批（A12·B42·C33·D122，待人工校准） |
| **SQL** | **156** | **本会话完成首批（A26·B48·C70·D12，待人工校准）** |
| **JS** | **118** | **本会话完成首批（A15·B42·C56·D5，待人工校准）** |
| **Linux** | **54** | **本会话完成首批（A2·B10·C37·D5，待人工校准）** |
| 其余小方向 | 33 | 移动端/UI设计/guava/python/scala 等，随对应方向合并处理 |

顶层 `AI`（148 条）与 `架构`（126 条）目录与技术方向强相关，本会话一并完成首批铺开。

## 3. 分级标准

| 级别 | 定义 | 处置 |
|---|---|---|
| A | 高价值必读：官方文档、体系化深度长文（原理/源码/集群/分片/最佳实践） | 人工确认后转写为知识库笔记 |
| B | 有价值深读：具体技术主题的优质文章 | 按主题分组，作为延伸阅读导航 |
| C | 参考速查：安装、配置、单点技巧、排错 | 保留速查，不转写 |
| D | 低价值：错放、近似重复、缓存/死链平台、论坛问答、聚合站 | 人工确认后从浏览器删除 |

## 4. 分级流水线

三个脚本（`scripts/`），可对任意方向复用：

```bash
# 1. 结构画像：顶层/方向分布
python3 scripts/analyze_structure.py source/bookmarks.html

# 2. 方向分级：抽取 方向/<名称>，去重后 A/B/C/D 分级，输出 data/<方向>.json
python3 scripts/bookmark_scale.py SQL

# 3. 渲染清单：生成 guide/<方向>.mdx（概览 + A 转写清单 + B/C 主题导航 + D 清理建议）
python3 scripts/render_scale.py SQL
```

分级规则（`bookmark_scale.py`，机器首轮、规则可迭代）：

- **D 信号**：错放关键词（按方向配置）、低价值平台（百度快照/文库、CSDN 论坛、新浪博客、IT 屋、docs4dev 镜像等）、同归一化标题重复、同文件夹对比类超 2 篇；
- **A 信号**：官方文档域名白名单（无操作类关键词干扰）、标题深度信号（原理/源码/体系/集群/分片/最佳实践/3W字等）；产品首页一律降 B；URL 带广告投放跟踪参数（bd_vid/utm_source=baidu 等）的厂商营销落地页不进 A/B；
- **C 信号**：安装/部署/配置/报错/速查类关键词；
- **兜底**：具体子分类下 → C，无主题散链 → D。

## 5. 铺开期进度

### SQL 方向 · 首批（2026-09-05）

156 条全部去重，分级 **A26 · B48 · C70 · D12**，产物：

- `data/sql.json` —— 分级明细（含依据与原书签路径）
- `guide/sql.mdx` —— 筛选清单（A 级转写候选 checkbox + B/C 主题导航 + D 级清理建议）

主题分布：MongoDB（44）、PostgreSQL（30）、MySQL/Redis/MyBatis/OLAP/InfluxDB/数据库原理等。

**待人工校准要点**：

1. A 级中微信长文（clickhouse 3W字）与 CSDN 聚合文（Redis 总结精讲）是否值得转写，需人工判断成色；
2. A 级 ShardingSphere/TiDB DBA 课程属官方入口，转写时可只做笔记索引不搬运内容；
3. D 级 12 条（docs4dev 镜像 ×2、错放 npm/CountDownLatch、百度快照、CSDN 论坛等）确认后可整批删除。

### JS 方向 · 首批（2026-09-05）

118 条全部去重，分级 **A15 · B42 · C56 · D5**，产物 `data/js.json` + `guide/js.mdx`。

主题分布：Node.js（29）、Vue 生态（21）、TypeScript（14）、框架与 UI 库（14）、JavaScript（5）、数据可视化（4）等。

**待人工校准要点**：

1. A 级以官方文档为主（Node.js/TypeScript/Babel/Ant Design/egg/nest/Fastify），转写优先级最高的两篇深度文是「JS 万字总结」与「Node.js 垃圾回收与内存管理」；
2. 书签目录错放严重：axios/bluebird/DvaJS/UmiJS 挂在 `vue/` 下，qiankun/Arco Design/Fastify 挂在 `框架/nest/` 下——归档时可顺手归位；
3. D 级 5 条（掘金话题页、前端之家聚合站、CSDN 转载站、qiankun 重复收藏、模板之家）确认后整批删除。

### Linux 方向 · 首批（2026-09-05）

54 条全部去重，分级 **A2 · B10 · C37 · D5**，产物 `data/linux.json` + `guide/linux.mdx`。

Linux 书签以运维操作/速查为主（C 级 37 条占七成），A 级仅 2 条（RAID 技术全解、阿里云内核网络参数），符合数据本性。

**待人工校准要点**：

1. C 级 37 条多为单命令速查（chmod/iftop/du/vmstat），与站内 `tools/basic/cli` 方向重叠，建议不再单独转写；
2. `源-镜像仓库/` 下的 Maven/docker Hub 属跨方向错放，归档时可移至对应工具方向；
3. D 级 5 条（百家号/爱码网/走看看/书签地球营销页/电子新闻页）确认后整批删除。

### AI 方向 · 首批（2026-09-05）

148 条，隐私剔除 27 条（公司内网 multica/GitNexus/Friday 平台与本地 `file://` 链接，不入公开产物），去重后 121 条分级 **A10 · B60 · C47 · D4**，产物 `data/ai.json` + `guide/ai.mdx`。

主题分布：业务 Agent（8）、AI 网站与模型（27）、Skills 生态、Coding Plan 订阅（9）、OpenClaw 生态（散布于 B/C 组）等。

**待人工校准要点**：

1. A 级成色混杂：kimi/Qoder/AgentTARS/LangChain 为官方文档入口；「Learn Claude Code」（第三方教程站）与「MCP 广场 · 魔搭」（市场页）需人工判断是否值得转写；
2. B 级「其他」组 27 条中 OpenClaw 生态相关占多数，转写时可聚合为一篇 OpenClaw 生态索引笔记；
3. D 级 4 条（RocketMQ 错放、火山方舟控制台近似重复、dxmcs 代购站 ×2）确认后整批删除；
4. 公司 AI 平台链接已按隐私规则整体剔除，不入任何产物；如需内网保留，从浏览器原书签单独导出即可。

### 架构 方向 · 首批（2026-09-05）

126 条全部去重，分级 **A11 · B60 · C47 · D8**，产物 `data/架构.json` + `guide/架构.mdx`。（首轮 A13，两条带百度投放参数的厂商营销落地页已按规则降 C。）

主题分布：微服务（10）、API 规范（13）、快速开发框架（9）、外包平台（14）、产品/运营（7）、设计模式（5）、权限方案（4）等。

**待人工校准要点**：

1. 真正值得转写的是 Seata 三种模式详解与 4 篇分布式事务/缓存体系长文；青云/深信服混合云营销落地页已降 C，如需混合云主题可替换为对应厂商官方文档入口；
2. 外包平台（B11 · C3）与产品经理/运营类目偏职业资源，与架构技术相关度低，建议整组降 D 或单独归档；
3. D 级 8 条（百度系营销页 ×5、网安问答、百度文库 ×2）确认后整批删除。

### Java 方向 · 首批（原会话）

原会话（bookmark-kb-design 工作区）已完成：A12·B42·C33·D122（209/215 去重），产物在原工作区 `guide/java.mdx`。本仓库不重复生成，避免双口径。

## 6. 下一步

1. 五方向首批人工校准（SQL/JS/Linux/AI/架构；Java 在原工作区已完成）：A 级确认 → 转写笔记；D 级确认 → 浏览器清理；
2. `方向` 目录与顶层 AI/架构铺开完成，评估 `website`/`阅读`/`技术` 等大目录的二级画像，决定第二铺开批次；
3. 剩余小方向（移动端/UI设计/guava/python/scala 等 33 条）随对应方向笔记补全时合并处理。
