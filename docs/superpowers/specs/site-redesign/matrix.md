# 全站重设计 v2 · 状态矩阵

Widget：`after/widget.html`。顶部状态切换器或 hash 直达；深/浅为主题横切维度（默认深色）。

| 功能点 \ 状态 | 首页浅色 | 首页深色 | 路线图 | 分类目录页 | 侧边栏层级 | 标记交互 | 移动端 |
|---|---|---|---|---|---|---|---|
| F1 五级信息架构 | `#light` 仪表盘 | `#dark` 同构 | `#roadmap` 等级分段+分类入口 | `#category` 面包屑 Java/高级/多线程并发 | `#tree` 方向→等级→分类→知识点 | — | `#mobile` 抽屉入口 |
| F2 黑白双主题 | `#light` 纯白底 | `#dark` 纯黑底（默认） | `#roadmap` 切换复核 | `#category` 切换复核 | — | — | `#mobile` 两主题复核 |
| F3 灰阶无彩色 | `#light` 链接=近黑+下划线 | `#dark` 链接=近白+下划线 | — | — | — | `#mark` 状态圆点三态图形 | — |
| F4 字重调轻 | `#light` H1 600/H2 500/正文 400 | `#dark` 同 | `#roadmap` 分段头 500 | `#category` 分类标题 600/条目 400 | — | — | — |
| F5 学习仪表盘 | `#light` 继续学习大卡+方向进度+下一步 | `#dark` 同 | — | — | — | — | `#mobile` 三块纵向排布 |
| F6 等级分段路线图 | — | — | `#roadmap` 三段+分类可点击入口+计数 | — | — | — | `#mobile` 路线图单列 |
| F6a 分类目录页 | — | — | — | `#category` 简介+知识点全列表（核心标记/简述/圆点/学习顺序） | — | — | `#mobile` 列表单列 |
| F7 状态标记交互 | — | — | `#roadmap` 条目圆点 | `#category` 知识点圆点 | — | `#mark` 点击循环 ○→◐→●（含 aria-label） | `#mobile` 热区 ≥32px |
| F8 侧边栏五级树 | — | — | — | — | `#tree` Java 全展开+分类节点可点击+当前页高亮+焦点环 | — | `#mobile` 收为抽屉 |

## 复核记录

| 日期 | 视口 | 主题 | 结果 |
|---|---|---|---|
| 2026-09-04 | 1280×800 / 375×812 | 浅 / 深 | r1 首次建档 |
| 2026-09-04 | 1280×800 | 浅 / 深 | r2 层级修正为五级（分类=目录页），浏览器验证通过：#category 路由/圆点三态循环/双主题切换/路线图段计数（3/3、0/1、2/10）与分类链接跳转/console 无报错；待用户审批 |
