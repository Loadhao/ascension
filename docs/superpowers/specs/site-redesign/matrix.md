# 全站重设计 v2 · 状态矩阵

Widget：`after/widget.html`。顶部状态切换器或 hash 直达；深/浅为主题横切维度（默认深色）。

| 功能点 \ 状态 | 首页浅色 | 首页深色 | 路线图 | 侧边栏层级 | 标记交互 | 移动端 |
|---|---|---|---|---|---|---|
| F1 四级信息架构 | `#light` 仪表盘顶部方向徽标 | `#dark` 同构 | `#roadmap` 等级分段+分类分组 | `#tree` 方向→等级→分类→笔记全展开 | — | `#mobile` 抽屉入口 |
| F2 黑白双主题 | `#light` 纯白底 | `#dark` 纯黑底（默认） | `#roadmap` 切换复核 | — | — | `#mobile` 两主题复核 |
| F3 灰阶无彩色 | `#light` 链接=近黑+下划线 | `#dark` 链接=近白+下划线 | — | — | `#mark` 状态圆点三态图形 | — |
| F4 字重调轻 | `#light` H1 600/H2 500/正文 400 | `#dark` 同 | `#roadmap` 分段头 500 | — | — | — |
| F5 学习仪表盘 | `#light` 继续学习大卡+方向进度+下一步 | `#dark` 同 | — | — | — | `#mobile` 三块纵向排布 |
| F6 等级分段路线图 | — | — | `#roadmap` 基础/中级/高级三段，Java 高级下分 JVM/多线程并发/内存池 | — | — | `#mobile` 路线图单列 |
| F7 状态标记交互 | — | — | `#roadmap` 条目圆点 | — | `#mark` 点击循环 ○→◐→●（含 aria-label） | `#mobile` 热区 ≥32px |
| F8 侧边栏四级树 | — | — | — | `#tree` Java 全展开 + 当前页高亮 + 焦点环 | — | `#mobile` 收为抽屉 |

## 复核记录

| 日期 | 视口 | 主题 | 结果 |
|---|---|---|---|
| 2026-09-04 | 1280×800 / 375×812 | 浅 / 深 | 首次建档，待用户审批 |
