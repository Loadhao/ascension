# 方向入口卡片 · 状态矩阵

> **EXAMPLE**：演示 `matrix.md` 写法。每格标明可证明该状态的位置：Widget hash 路由或画面区域。真实资产同样不留未说明状态。

Widget：`after/widget.html`，通过顶部状态切换器或 hash 直达；主题（浅/深）为横切维度，两主题下各状态均需复核。

| 功能点 \ 状态 | 正常 | 悬停 | 键盘焦点 | 禁用 | 移动窄屏 |
|---|---|---|---|---|---|
| F1 方向卡片网格 | `#normal` 三列网格 | — | — | — | `#mobile` 单列全宽 |
| F2 悬停反馈 | — | `#hover`（Linux 卡预置悬停样式） | — | — | — |
| F3 键盘可达 | — | — | `#focus`（Java 卡焦点环，Tab 顺序 = 视觉顺序） | — | `#mobile`（触摸无焦点环，仅按压反馈） |
| F4 未开放方向 | `#normal` 不出现 | — | — | `#disabled`（「数据库 · 即将上线」卡：60% 透明、不可聚焦、光标禁用） | `#mobile` 同形态单列 |
| F5 学习进度提示 | `#normal` 各卡右下徽章（篇数 + 学习中） | `#hover` 徽章随卡强调 | — | `#disabled` 占位卡显示「0 篇」 | `#mobile` 徽章同行不换行 |

## 复核记录

| 日期 | 视口 | 主题 | 结果 |
|---|---|---|---|
| 2026-09-04 | 1280×800 / 375×812 | 浅 / 深 | 示例资产，首次建档 |
