{
  "questions": [
    {
      "id": "vue-react-001",
      "noteId": "vue/basic/core/01-reactivity",
      "type": "single",
      "difficulty": 3,
      "q": "Vue 3 响应式改用 Proxy 后，Vue 2 的哪些历史坑被根除？",
      "options": [
        "对象新增/删除属性、数组下标赋值都能被劫持并触发更新",
        "模板编译不再需要构建步骤",
        "组件不再需要 data 函数返回对象",
        "computed 从此没有缓存"
      ],
      "answer": [
        0
      ],
      "hint": "defineProperty 只能劫持初始化时存在的属性；Proxy 拦截一切操作（含新增/删除/下标），代价是放弃 IE11。"
    },
    {
      "id": "vue-destr-002",
      "noteId": "vue/basic/core/01-reactivity",
      "type": "single",
      "difficulty": 4,
      "q": "reactive 对象解构出 name 后再赋值，界面不更新，原因是？",
      "options": [
        "解构出来的是普通值，脱离了 Proxy 代理，依赖收集已断开",
        "解构触发了 Proxy 的删除陷阱",
        "Vue 的依赖表容量超限，静默失效",
        "需要把 reactive 换成 ref 才能解构"
      ],
      "answer": [
        0
      ],
      "hint": "响应性挂在 Proxy 的 get/set 上：解构等于把值抄走；要保响应用 toRefs 或始终经对象访问。"
    },
    {
      "id": "vue-mixin-003",
      "noteId": "vue/basic/core/03-composition-api",
      "type": "single",
      "difficulty": 3,
      "q": "组合式函数取代 mixin 的核心优势是？",
      "options": [
        "普通函数显式 import：来源清晰、命名隔离、类型可推导",
        "运行时性能比 mixin 快一个数量级",
        "只能在 <script setup> 中使用从而限制滥用",
        "mixin 在 Vue 3 中已被移除"
      ],
      "answer": [
        0
      ],
      "hint": "mixin 的属性混进 this：来源不明、撞名静默覆盖、TS 难推导；组合式函数回到函数组合的朴素复用。"
    },
    {
      "id": "vue-proxy-004",
      "noteId": "vue/basic/core/01-reactivity",
      "type": "single",
      "q": "Vue 3 响应式为什么用 Proxy 替代 Vue 2 的 Object.defineProperty？reactive 解构要注意什么？",
      "options": [
        "Proxy 拦截一切操作（新增/删除属性、数组下标），根除 Vue 2 靠 $set 补丁、数组下标赋值监听不到的历史包袱；但 reactive 解构出来的是普通值会丢失响应性，需 toRefs 或保持对象访问",
        "Proxy 只是为了性能，Vue 2 的 defineProperty 本来就能监听新增属性",
        "解构后的变量仍是响应式的，Vue 会自动重新包裹",
        "ref 与 reactive 完全等价，只是命名风格不同"
      ],
      "answer": [
        0
      ],
      "hint": "依赖收集是「组件渲染读到 state.count 时记进依赖表」，修改时按表精准通知——渲染粒度细的根源；ref 是基本类型的包装（.value 是依赖收集的钩子）；与 React 对照记：推模式 vs 拉模式。vue 方向首题。",
      "difficulty": 3
    },
    {
      "id": "vue-dataflow-005",
      "noteId": "vue/basic/core/02-component-model",
      "type": "single",
      "q": "Vue 的单向数据流与 v-model 的关系，按笔记正确的是？",
      "options": [
        "子组件不能改 props（数据只有所有者能改），要改就 emit 事件由父组件动手；v-model 只是「传 :modelValue + 监听 @update:modelValue」的语法糖，没有破坏单向数据流",
        "v-model 实现了真正的双向绑定，子组件可以直接修改父组件的数据",
        "props 是可变的引用，子组件修改后会自动同步回父组件",
        "v-model 只能用于原生 input 元素，自定义组件无法使用"
      ],
      "answer": [
        0
      ],
      "hint": "组件内 defineModel()（3.4+）可以少写样板、多个 v-model 用参数区分；跨多层传递用 provide/inject，别把 props 链拉成穿山隧道；模板编译为渲染函数并做静态提升——静态内容只创建一次。vue 方向第二题。",
      "difficulty": 3
    },
    {
      "id": "vue-context-006",
      "noteId": "vue/basic/core/03-composition-api",
      "type": "single",
      "q": "组合式函数 useMouse 里 onMounted/onUnmounted 注册了监听器，为什么在组件 setup 中调用它能正确清理？在异步回调里再注册生命周期钩子会怎样？",
      "options": [
        "生命周期钩子必须在 setup 同步执行时注册——此时「组件实例正在初始化」的上下文存在；组合式函数在 setup 里调用即拿到该上下文，异步回调里再注册会失联",
        "生命周期钩子是全局的，任何时机注册都能关联到最近的组件",
        "onUnmounted 的清理靠 GC 自动完成，无需显式注册",
        "组合式函数必须在 <script setup> 里定义才能使用生命周期"
      ],
      "answer": [
        0
      ],
      "hint": "组合式函数本质是普通函数——状态+生命周期打包复用（useMouse 返回 {x, y}）；按「功能」组织代码取代 Options API 按「类型」归堆。vue-mixin-003 考组合式优于 mixin 的原因，本题考 setup 上下文与注册时机。",
      "difficulty": 3
    }
  ]
}
