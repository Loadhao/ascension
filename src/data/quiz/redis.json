{
  "questions": [
    {
      "id": "redis-ds-001",
      "noteId": "redis/basic/core/01-data-structures",
      "type": "multiple",
      "difficulty": 4,
      "q": "关于 Redis 底层数据结构，下列说法正确的有？（多选）",
      "options": [
        "ZSet 大数据时 skiplist 与 dict 并存：按 member 查分数走 dict O(1)，按分数范围查走跳表",
        "跳表插入节点时随机决定层数，每层晋升概率 1/4、最高 64 层",
        "listpack 每个 entry 记录前一项目的长度，因此解决了 ziplist 的连锁更新",
        "渐进式 rehash 期间新增键只写入新表 ht[1]，查询则要先后查两张表"
      ],
      "answer": [
        0,
        1,
        3
      ],
      "hint": "第 3 项因果颠倒——连锁更新恰恰源于 ziplist 记「前一项长度」；listpack 根除问题的原因是只记自己的长度。"
    },
    {
      "id": "redis-rdb-002",
      "noteId": "redis/basic/core/02-persistence",
      "type": "single",
      "difficulty": 4,
      "q": "bgsave 期间主进程仍持续处理写请求，子进程却能拍到「一致」的快照，靠的是什么机制？",
      "options": [
        "主进程在快照期间暂停所有写操作，写请求排队",
        "操作系统写时复制（COW）：主进程改某页时操作系统复制该页，子进程看到的仍是 fork 时刻的旧页",
        "子进程通过 AOF 缓冲区实时同步主进程的新写命令",
        "Redis 加了全局互斥锁，快照结束后才放行写请求"
      ],
      "answer": [
        1
      ],
      "hint": "快照期间主进程既不停写也没有加锁；COW 的代价是「改多少页复制多少页」，大实例快照期间内存可能接近翻倍，这才是部署容量要留余量的原因。"
    },
    {
      "id": "redis-aof-003",
      "noteId": "redis/basic/core/02-persistence",
      "type": "judge",
      "difficulty": 4,
      "q": "AOF 重写（bgrewriteaof）是对旧 AOF 文件里的命令逐条压缩合并，生成更短的命令序列。",
      "options": [
        "正确",
        "错误"
      ],
      "answer": [
        1
      ],
      "hint": "重写的输入不是旧 AOF 文件，而是当前内存状态——fork 子进程反向生成最小等价命令集（如 100 次 incr 合并成一条 set counter 100），与 RDB 一样依赖 fork+COW。"
    },
    {
      "id": "redis-bloom-004",
      "noteId": "redis/intermediate/usage/02-cache-problems",
      "type": "single",
      "difficulty": 3,
      "q": "布隆过滤器「判不存在绝对可靠、判存在有误判率」的性质，为什么恰好适合防缓存穿透？",
      "options": [
        "穿透请求查的都是不存在的数据，判不存在绝对可靠能全部挡下，误判放行的极少数走正常链路兜底",
        "误判率的存在让布隆过滤器可以动态扩容，所以容量无需预估",
        "被判为存在的数据可以直接从布隆过滤器里取值返回，不必再查缓存和 DB",
        "误判率使它能顺带识别出热点 key，兼防击穿"
      ],
      "answer": [
        0
      ],
      "hint": "容量恰恰要预估、且单个元素不能删除；布隆过滤器里只有位数组没有数据本体，判为「存在」仍要放行去查缓存和 DB——它的价值就落在「穿透的数据确定不存在」与「判不存在绝对可靠」的精确咬合上。"
    },
    {
      "id": "redis-thread-005",
      "noteId": "redis/basic/core/03-thread-model",
      "type": "single",
      "difficulty": 3,
      "q": "Redis 6.0 开启 IO 多线程后，命令执行的线程模型是？",
      "options": [
        "多条命令在 IO 线程并行执行，原子性不再保证",
        "IO 线程只分担读写与协议解析，命令执行仍回单线程排队",
        "每个 CPU 核一个执行线程，靠锁保护 dict",
        "执行也并行，靠 MVCC 保证可见性"
      ],
      "answer": [
        1
      ],
      "hint": "「单线程」从来只指命令执行；6.0 解决的是 socket/解析慢，不是计算并行。"
    },
    {
      "id": "redis-expire-006",
      "noteId": "redis/intermediate/usage/01-expiration-eviction",
      "type": "multiple",
      "difficulty": 3,
      "q": "关于过期删除与内存淘汰，下列正确的有？（多选）",
      "options": [
        "过期采用惰性删除 + 定期删除，不是到点立刻删",
        "内存满时的默认策略是 allkeys-lru",
        "noeviction 在内存满时拒绝写入",
        "8 种淘汰策略里，volatile-* 只从设置了过期的键里挑"
      ],
      "answer": [
        0,
        2,
        3
      ],
      "hint": "默认是 noeviction 不是 LRU；把过期和淘汰当成一回事会配错 maxmemory-policy。"
    },
    {
      "id": "redis-lock-007",
      "noteId": "redis/intermediate/usage/03-distributed-lock",
      "type": "single",
      "difficulty": 3,
      "q": "setnx 成功后再单独 expire，最大的风险是？",
      "options": [
        "expire 会覆盖已有 TTL 导致锁永不过期？相反：两步不原子，崩溃在中间会死锁",
        "Redis 单线程保证了两步一定一起成功",
        "value 不用唯一标识也可以安全 del",
        "这个问题只在 Cluster 才存在"
      ],
      "answer": [
        0
      ],
      "hint": "正解是 SET NX EX 一条命令；释放必须 Lua 校验唯一 token，防止删掉别人续上的锁。"
    },
    {
      "id": "redis-repl-008",
      "noteId": "redis/advanced/ha/01-replication-sentinel-cluster",
      "type": "single",
      "difficulty": 2,
      "q": "Redis 高可用三层递进，正确对应是？",
      "options": [
        "主从复制冗余 → 哨兵自动故障转移 → Cluster 16384 槽分片",
        "Cluster 只做高可用不做分片，哨兵才做槽迁移",
        "哨兵负责分片，主从负责选主",
        "三层必须同时开启才有主从"
      ],
      "answer": [
        0
      ],
      "hint": "先复制、再自动切主、最后才水平分片；不要把哨兵和 Cluster 的职责对调。"
    },
    {
      "id": "redis-pattern-009",
      "noteId": "redis/intermediate/usage/04-cache-patterns",
      "type": "single",
      "difficulty": 5,
      "q": "Cache Aside 写路径的主流顺序是「先更新 DB 再删缓存」。若改成「先删缓存再更新 DB」，最典型的脏数据窗口是？",
      "options": [
        "删缓存失败会导致 DB 永久写不进去",
        "删后、写库完成前的并发读会把旧值回填进缓存，脏到下次删除",
        "Redis 单线程会自动串行化这次读写，所以没有窗口",
        "只能在 Cluster 分片下出现，单机 Redis 安全"
      ],
      "answer": [
        1
      ],
      "hint": "先删再写的窗口里，读 miss 会把尚未更新的旧行回填；主流组合是先更库再删 + 删除失败进重试/binlog 补偿。要强一致的数据诚实回答是：不适合缓存。"
    },
    {
      "id": "redis-pipe-010",
      "noteId": "redis/intermediate/usage/05-pipeline-transaction-lua",
      "type": "multiple",
      "difficulty": 4,
      "q": "关于 Pipeline、MULTI/EXEC 与 Lua，下列正确的有？（多选）",
      "options": [
        "Pipeline 把多条命令打包往返，服务端仍逐条执行，中间可能被其他客户端插队",
        "EXEC 后某条命令失败会自动回滚已执行的命令，保证全成或全败",
        "WATCH 在 EXEC 前发现被监视 key 被改过，整个事务放弃（返回 nil）",
        "集群上 Lua 访问的所有 key 必须落在同一槽，可用 hash tag 强制同槽"
      ],
      "answer": [
        0,
        2,
        3
      ],
      "hint": "Redis 事务不支持回滚——EXEC 时某条失败其余照常生效；入队阶段的语法错误才会整体放弃。真正「多命令+判断」原子靠 Lua，且脚本必须短小，否则单线程假死。"
    },
    {
      "id": "redis-bigkey-011",
      "noteId": "redis/intermediate/usage/06-bigkey-hotkey",
      "type": "single",
      "difficulty": 4,
      "q": "线上要找大 key，为什么生产环境几乎禁止用 MONITOR，而推荐 redis-cli --bigkeys 或离线分析 RDB？",
      "options": [
        "MONITOR 只能看过期键，看不到大 value",
        "MONITOR 本身会独占单线程执行路径，生产等于给自己制造阻塞",
        "--bigkeys 会 KEYS * 扫全库，MONITOR 反而更安全",
        "RDB 分析必须停写，所以只适合开发环境"
      ],
      "answer": [
        1
      ],
      "hint": "单线程公理：MONITOR / KEYS * / DEL 百万元素都会卡住所有请求。--bigkeys 是 SCAN 式采样；精确盘点离线解析 RDB 零线上风险。删除大集合用 UNLINK 而不是 DEL。"
    },
    {
      "id": "redis-watchdog-012",
      "noteId": "redis/intermediate/usage/03-distributed-lock",
      "type": "single",
      "q": "关于 Redisson 看门狗的续期机制，按笔记正确的是？",
      "options": [
        "lock() 不指定 leaseTime 才启用看门狗：后台定时任务每 10s（TTL 30s 的 1/3）检查仍持有就把 TTL 重置回 30s；unlock 或客户端崩溃后停止续期（崩溃最多 30s 自动释放）",
        "看门狗默认关闭，必须手动传 leaseTime 才会启动续期",
        "看门狗续期依赖主从同步，从库也会各自为锁续期",
        "客户端崩溃后看门狗仍在后台无限续期，直到人工清理"
      ],
      "answer": [
        0
      ],
      "hint": "「不传 leaseTime 才启用」是最常考的反直觉点；可重入靠 Hash 结构记重入计数（同线程 lock +1、unlock -1 归零才真删，全程 Lua 保证原子）；主从切换有丢锁窗口——正确性攸关场景用 DB 唯一约束或 ZooKeeper 兜底。",
      "difficulty": 4
    },
    {
      "id": "redis-sds-013",
      "noteId": "redis/basic/core/01-data-structures",
      "type": "single",
      "q": "Redis 的 SDS 相比 C 字符串的三大改造，按笔记正确的是？",
      "options": [
        "len 字段 O(1) 取长度；按 len 而非 '\\0' 判结尾（二进制安全，图片/序列化字节流都能存）；空间预分配 + 惰性释放减少反复 realloc",
        "用哈希表管理字符，任意前缀查找 O(1)",
        "只支持 ASCII，遇到非 ASCII 字符自动转 UTF-8 缓冲",
        "内容强制以 '\\0' 结尾且不能包含 '\\0'，与 C 字符串行为完全一致"
      ],
      "answer": [
        0
      ],
      "hint": "「二进制安全」的含义就是内容里可以有 '\\0'——判结尾看 len 不看 '\\0'；预分配/惰性释放把反复扩缩容的 realloc 摊薄。redis-ds-001 考编码切换与 rehash，本题考 SDS 本体。",
      "difficulty": 3
    },
    {
      "id": "redis-slowcmd-014",
      "noteId": "redis/basic/core/03-thread-model",
      "type": "single",
      "q": "Redis 实例偶发百毫秒级卡顿，结合「单线程」公理，可能的原因与优化方向按笔记正确的是？",
      "options": [
        "单线程下任何慢操作都阻塞所有请求：KEYS */百万元素 SMEMBERS 卡住全部命令（换 SCAN 渐进扫描）；大内存实例 fork 做持久化是毫秒~百毫秒级停顿；吞吐顶不住时一机多实例绑核而不是调参数",
        "卡顿一定是网络抖动，与命令类型无关",
        "多开几个 io-threads 就能并行执行慢命令，卡顿自然消失",
        "fork 的写时复制不占额外内存，大实例持久化零成本"
      ],
      "answer": [
        0
      ],
      "hint": "一切性能优化回到单线程公理——别让任何一条命令跑太久；4.0 的 UNLINK/异步 FLUSHALL 就是把大 key 释放挪出主线程；io-threads 只分担读写解析，执行仍排队。redis-thread-005 考 6.0 IO 多线程模型，本题考单线程的三个瓶颈。",
      "difficulty": 4
    },
    {
      "id": "redis-lua-015",
      "noteId": "redis/intermediate/usage/05-pipeline-transaction-lua",
      "type": "single",
      "q": "读-改-写场景（先 GET 判断再 INCR）为什么推荐用 Lua 而不是 MULTI/EXEC 事务？",
      "options": [
        "Lua 脚本整段原子执行，还能在脚本里写判断逻辑（读值再决定写什么）——MULTI/EXEC 只是命令打包顺序执行、不保证中间不被插队，也没有 if 分支能力",
        "Lua 脚本执行更快是因为它跳过了命令解析环节",
        "MULTI/EXEC 事务有回滚能力，Lua 反而没有",
        "Lua 脚本在集群上可以访问任意槽位的 key"
      ],
      "answer": [
        0
      ],
      "hint": "Redis 事务没有回滚（只有 EXEC 前入队错误才整队放弃）；集群上 Lua 访问的所有 key 必须落在同一槽（hash tag 强制）；Pipeline 只省往返、不保证原子。redis-pipe-010 多选考三者边界，本题考 Lua 的核心价值：原子 + 可编程。",
      "difficulty": 3
    },
    {
      "id": "redis-hotrebuild-016",
      "noteId": "redis/intermediate/usage/02-cache-problems",
      "type": "single",
      "q": "热点 key 击穿的两种方案——互斥锁重建与逻辑过期，按笔记的取舍正确的是？",
      "options": [
        "互斥锁简单但等待会堆积（没抢到的 sleep 重试）；逻辑过期 key 永不设 TTL、过期时间写进 value，抢到锁的异步重建、没抢到的先返回旧数据——牺牲一致性保吞吐，热点明确（秒杀商品）用它更顺",
        "逻辑过期没有一致性代价，是互斥锁的严格升级版",
        "互斥锁方案里所有请求都会去查库，只是先后有序",
        "逻辑过期的 key 不设 TTL 是为了省一次 EXPIRE 命令"
      ],
      "answer": [
        0
      ],
      "hint": "double check 是互斥方案的必需步骤（等锁期间别人可能已重建）；随机 TTL 打散治的是雪崩——击穿是单个热点，别混淆。redis-bloom-004 考穿透与布隆过滤器，本题考击穿的两种重建策略。",
      "difficulty": 4
    },
    {
      "id": "redis-sentinelconf-017",
      "noteId": "redis/advanced/ha/01-replication-sentinel-cluster",
      "type": "single",
      "q": "哨兵的两个容易混的数（quorum 与 leader 选举）以及脑裂残留风险的缓解参数，按笔记正确的是？",
      "options": [
        "quorum 只用于「认定客观下线」（门槛可低）；leader 选举必须过半哨兵 majority——所以哨兵部署奇数个 ≥3；脑裂缓解靠 min-replicas-to-write/min-replicas-max-lag 让主库在失联时拒绝写入",
        "quorum 用于选 leader，majority 用于认定客观下线，两者含义相同",
        "脑裂无法缓解，分区期间的写入丢失只能靠人工对账修复",
        "哨兵部署 2 个就够，多数派决策天然成立"
      ],
      "answer": [
        0
      ],
      "hint": "挑新主四步：排除断线 → replica-priority 优先级 → 复制 offset 最新 → runid 最小；Cluster 用 16384 槽 + MOVED/ASK 重定向 + Gossip 探活。redis-repl-008 考三层递进，本题考哨兵细节与脑裂缓解。",
      "difficulty": 4
    },
    {
      "id": "redis-watchdog-018",
      "noteId": "redis/intermediate/usage/03-distributed-lock",
      "type": "single",
      "difficulty": 4,
      "q": "业务预计运行 20 秒，Redisson 调用 lock(10, SECONDS) 后仍未结束；看门狗对锁的行为是什么？",
      "options": [
        "看门狗会忽略显式租期并每 10 秒续到 30 秒",
        "显式 leaseTime 不启动看门狗，锁在 10 秒后过期，业务仍运行时互斥可能失效",
        "看门狗会在 unlock 时才启动，提前延长锁寿命",
        "Redis 会根据业务线程是否存活自动延长 TTL"
      ],
      "answer": [
        1
      ],
      "hint": "只有不指定 leaseTime 的 lock() 才交给看门狗管理 TTL；显式租期表示调用方承诺过期时间，业务超时后锁仍会释放。redis-watchdog-012 考是否启用与崩溃兜底，本题考显式租期造成的互斥边界。"
    },
    {
      "id": "redis-redisson-019",
      "noteId": "redis/intermediate/usage/07-redisson",
      "type": "multiple",
      "difficulty": 4,
      "q": "用 Redisson 做跨实例并发控制，哪些说法成立？",
      "options": [
        "RSemaphore 的许可不会自动过期，持有者进程崩溃会让许可永久泄漏；需要租约就用 RPermitExpirableSemaphore，并按 acquire 返回的 id 释放",
        "配限速用 trySetRate（仅在未配置时生效），setRate 会覆盖已有配置并重置已消耗的令牌",
        "RFencedLock 给出单调递增 token，只要客户端自己比较 token 就能挡住过期后复活的旧持有者",
        "读写锁能降低读路径开销，因为读锁在客户端本地判定、不访问 Redis"
      ],
      "answer": [
        0,
        1
      ],
      "hint": "fencing 的价值全在「资源侧记住见过的最大 token 并拒绝更小的」——只在客户端比等于没做；被保护的资源若无法校验 token（比如一个不认识 token 的第三方接口），这套就退化成普通锁。Redisson 的同步器都是 Redis 侧 Lua 原子脚本的封装，读写锁每次 acquire/release 都是一次网络往返，热点读路径上常比无锁更慢。锁本身仍受主从异步切换丢锁影响，见分布式锁那篇。"
    }
  ]
}
