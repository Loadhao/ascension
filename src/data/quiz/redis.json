{
  "questions": [
    {
      "id": "redis-ds-001",
      "noteId": "redis/basic/core/01-data-structures",
      "type": "multiple",
      "q": "关于 Redis 底层数据结构，下列说法正确的有？（多选）",
      "options": [
        "ZSet 大数据时 skiplist 与 dict 并存：按 member 查分数走 dict O(1)，按分数范围查走跳表",
        "跳表插入节点时随机决定层数，每层晋升概率 1/4、最高 64 层",
        "listpack 每个 entry 记录前一项目的长度，因此解决了 ziplist 的连锁更新",
        "渐进式 rehash 期间新增键只写入新表 ht[1]，查询则要先后查两张表"
      ],
      "answer": [
        0,
        1,
        3
      ],
      "hint": "第 3 项因果颠倒——连锁更新恰恰源于 ziplist 记「前一项长度」；listpack 根除问题的原因是只记自己的长度。"
    },
    {
      "id": "redis-rdb-002",
      "noteId": "redis/basic/core/02-persistence",
      "type": "single",
      "q": "bgsave 期间主进程仍持续处理写请求，子进程却能拍到「一致」的快照，靠的是什么机制？",
      "options": [
        "主进程在快照期间暂停所有写操作，写请求排队",
        "操作系统写时复制（COW）：主进程改某页时操作系统复制该页，子进程看到的仍是 fork 时刻的旧页",
        "子进程通过 AOF 缓冲区实时同步主进程的新写命令",
        "Redis 加了全局互斥锁，快照结束后才放行写请求"
      ],
      "answer": [
        1
      ],
      "hint": "快照期间主进程既不停写也没有加锁；COW 的代价是「改多少页复制多少页」，大实例快照期间内存可能接近翻倍，这才是部署容量要留余量的原因。"
    },
    {
      "id": "redis-aof-003",
      "noteId": "redis/basic/core/02-persistence",
      "type": "judge",
      "q": "AOF 重写（bgrewriteaof）是对旧 AOF 文件里的命令逐条压缩合并，生成更短的命令序列。",
      "options": [
        "正确",
        "错误"
      ],
      "answer": [
        1
      ],
      "hint": "重写的输入不是旧 AOF 文件，而是当前内存状态——fork 子进程反向生成最小等价命令集（如 100 次 incr 合并成一条 set counter 100），与 RDB 一样依赖 fork+COW。"
    },
    {
      "id": "redis-bloom-004",
      "noteId": "redis/intermediate/usage/02-cache-problems",
      "type": "single",
      "q": "布隆过滤器「判不存在绝对可靠、判存在有误判率」的性质，为什么恰好适合防缓存穿透？",
      "options": [
        "穿透请求查的都是不存在的数据，判不存在绝对可靠能全部挡下，误判放行的极少数走正常链路兜底",
        "误判率的存在让布隆过滤器可以动态扩容，所以容量无需预估",
        "被判为存在的数据可以直接从布隆过滤器里取值返回，不必再查缓存和 DB",
        "误判率使它能顺带识别出热点 key，兼防击穿"
      ],
      "answer": [
        0
      ],
      "hint": "容量恰恰要预估、且单个元素不能删除；布隆过滤器里只有位数组没有数据本体，判为「存在」仍要放行去查缓存和 DB——它的价值就落在「穿透的数据确定不存在」与「判不存在绝对可靠」的精确咬合上。"
    }
  ]
}
