{
  "questions": [
    {
      "id": "mqtt-wildcard-001",
      "noteId": "mqtt/basic/core/01-mqtt-protocol",
      "type": "judge",
      "difficulty": 1,
      "q": "MQTT 订阅时，多层通配符 # 可以放在主题过滤器的任意层级，例如 sensors/#/temp 是合法订阅。",
      "options": [
        "正确",
        "错误"
      ],
      "answer": [
        1
      ],
      "hint": "# 只能结尾，单层通配是 +；另外主题是逻辑路由约定而非目录，不能拿 URL 路径的层级直觉来套。"
    },
    {
      "id": "mqtt-qos0-002",
      "noteId": "mqtt/basic/core/02-qos-session",
      "type": "judge",
      "difficulty": 3,
      "q": "使用持久会话（cleanSession=false）时，Broker 也会为离线客户端缓存 QoS 0 消息，重连后统一补发。",
      "options": [
        "正确",
        "错误"
      ],
      "answer": [
        1
      ],
      "hint": "QoS 0 没有确认机制、根本不入会话队列，离线直接丢；离线补偿必须 QoS≥1，且重投队列还有配额上限，不是无限「邮局」。"
    },
    {
      "id": "mqtt-qos2-003",
      "noteId": "mqtt/basic/core/02-qos-session",
      "type": "single",
      "difficulty": 4,
      "q": "MQTT QoS 2 能做到「恰好一次」（不重不丢），靠的关键机制是？",
      "options": [
        "接收方对 PUBREC 过的 message id 记账，重复报文只回 ACK 不再投递；发送方未收到 PUBCOMP 时重发的是 PUBREL 而非 PUBLISH",
        "每条消息携带全局唯一 UUID，Broker 靠唯一索引兜底去重",
        "PUBLISH 报文内置时间戳，接收方丢弃超出重传窗口的重复包",
        "QoS 2 无需专门机制，靠 TCP 可靠传输天然保证应用层恰好一次"
      ],
      "answer": [
        0
      ],
      "hint": "把传输层可靠等同于消息层恰好一次是常见误区——重发仍会造成应用层重复；消重靠「中间态记账」：接收方按 message id 去重，发送方只重发确认报文、不重发数据。"
    },
    {
      "id": "mqtt-ka-004",
      "noteId": "mqtt/intermediate/usage/02-keepalive-reconnect",
      "type": "single",
      "difficulty": 2,
      "q": "MQTT Keep Alive 的作用是？",
      "options": [
        "保证 QoS 2 恰好一次",
        "CONNECT 声明的间隔内若无任何报文，Broker 发 PINGREQ 探活，失败则判定掉线并触发遗嘱",
        "把 QoS 0 消息缓存在 Broker 直到客户端上线",
        "替代 TLS 做加密"
      ],
      "answer": [
        1
      ],
      "hint": "Keep Alive 管活着，不管投递语义；离线缓存仍看 QoS 与会话。"
    },
    {
      "id": "mqtt-emqx-005",
      "noteId": "mqtt/intermediate/usage/01-broker-emqx",
      "type": "judge",
      "difficulty": 3,
      "q": "EMQX 这类 Broker 把海量长连接和主题路由从业务进程里拆出去，设备侧仍用轻量 MQTT，后端再用桥接进 Kafka 等管道。",
      "options": [
        "正确",
        "错误"
      ],
      "answer": [
        0
      ],
      "hint": "不要让业务 JVM 直接扛百万设备连接；Broker 是连接面，消息总线是数据面。"
    }
  ]
}
