{
  "questions": [
    {
      "id": "nginx-worker-001",
      "noteId": "nginx/basic/config/01-working-model",
      "type": "single",
      "difficulty": 3,
      "q": "关于 Nginx 的 worker 与连接模型，下列说法正确的是？",
      "options": [
        "worker_processes 应设为 CPU 核数的 2~4 倍，以利用 IO 等待时间",
        "worker_connections 是整个 Nginx 实例的最大连接数上限",
        "worker 是单线程 epoll 事件循环，向 upstream 发起连接后不干等 IO，转去处理其他连接",
        "多个 worker 之间可以靠共享内存变量实现跨请求状态（如共享计数器）"
      ],
      "answer": [
        2
      ],
      "hint": "worker 是单线程，核数才等于并行度，配多了反而上下文切换变多；worker_connections 是「每个 worker」的上限；worker 间默认不共享内存变量，跨请求状态要放 Redis/DB。"
    },
    {
      "id": "nginx-proxy-002",
      "noteId": "nginx/intermediate/proxy/01-reverse-proxy-lb",
      "type": "judge",
      "difficulty": 4,
      "q": "配置 location /api/ { proxy_pass http://backend; }（proxy_pass 不带尾斜杠）时，请求 /api/users 会被转发为 http://backend/users（location 前缀被去掉）。",
      "options": [
        "正确",
        "错误"
      ],
      "answer": [
        1
      ],
      "hint": "只有 proxy_pass 自身带了 URI 部分/尾斜杠才会替换前缀；不带斜杠时完整原始 URI 原样转发，与 location 写法无关——「代理后 404」多半是把这个决定因素安错了地方。"
    },
    {
      "id": "nginx-limit-003",
      "noteId": "nginx/intermediate/proxy/02-https-cache-ratelimit",
      "type": "multiple",
      "difficulty": 4,
      "q": "关于 Nginx 的缓存与限流，下列说法正确的有？（多选）",
      "options": [
        "limit_req 默认是漏桶形态，突发请求被压平、以恒定速率流出",
        "burst=20 nodelay 中的 nodelay 表示超出的请求先进入排队、稍后按速率慢慢放行",
        "限流超限后 Nginx 返回 503，在入口就把流量挡住",
        "缓存串数据的元凶通常是 cache_key 没带全影响结果的参数/头"
      ],
      "answer": [
        0,
        2,
        3
      ],
      "hint": "nodelay 的语义说反了——nodelay 是「积压也立刻放」（消耗 burst 配额），去掉 nodelay 才是排队限速流出。"
    },
    {
      "id": "nginx-static-004",
      "noteId": "nginx/basic/config/02-static-server",
      "type": "single",
      "difficulty": 3,
      "q": "location /static/ 配 alias /opt/assets/ 时，请求 /static/a.png 会落到？与 root 的差别是？",
      "options": [
        "/opt/assets/static/a.png，因为 alias 和 root 一样拼完整 URI",
        "/opt/assets/a.png：alias 替换 location 前缀；root 则是根路径拼上完整 URI",
        "/var/www/site/a.png，alias 被 root 覆盖",
        "只能用 try_files，alias 不能配静态目录"
      ],
      "answer": [
        1
      ],
      "hint": "root 追加完整 URI，alias 剥掉前缀再替换——混用是静态 404 的第一嫌疑。SPA 刷新还要靠 try_files 回退 index.html。"
    },
    {
      "id": "nginx-cdn-005",
      "noteId": "nginx/intermediate/proxy/04-cdn",
      "type": "judge",
      "difficulty": 2,
      "q": "CDN 把静态内容缓存在边缘节点，回源只在未命中时发生；动态 API 默认不该整站交给 CDN 当源站缓存。",
      "options": [
        "正确",
        "错误"
      ],
      "answer": [
        0
      ],
      "hint": "缓存键和 Cache-Control 决定能不能缓存；把带 Cookie 的个性化接口缓在边缘会串数据。"
    },
    {
      "id": "nginx-ka-006",
      "noteId": "nginx/intermediate/proxy/03-keepalived-ha",
      "type": "single",
      "difficulty": 4,
      "q": "keepalived 给 Nginx 做主备时，track_script 应该探什么？脑裂比宕机更危险是因为？",
      "options": [
        "探主机 ping 通即可；脑裂只是短暂丢几个包",
        "探 nginx 进程/本机健康检查：主机活着但 nginx 死了也要让位；脑裂会双主绑同一 VIP，ARP 抖动比宕机更难查",
        "探上游业务接口；脑裂时 VRRP 会自动 fencing 对端",
        "探 VIP 是否在本机；有状态的 MySQL 主从也该用同一套 VRRP 管选主"
      ],
      "answer": [
        1
      ],
      "hint": "weight 扣到低于备机 priority 才会切换。无状态入口用 keepalived 做 IP 漂移；有状态主从交给哨兵/ZK，VRRP 不懂复制。脑裂防护：独立心跳 + 仲裁 + fencing。"
    }
  ]
}
