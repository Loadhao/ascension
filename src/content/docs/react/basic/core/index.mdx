---
title: 核心概念
description: 声明式 UI、Hooks 心智模型、渲染与 diff：React 的三块地基
---

import CategoryNotesIsland from '../../../../../components/learn/CategoryNotesIsland.astro';

本分类是 React 的地基：**声明式范式**（UI = f(state)，对后端读者
类比 K8s 调谐循环）、**Hooks 心智模型**（快照语义与闭包陷阱）、
**渲染流程**（render → diff → commit 与 key 的意义）。掌握这三块，
组件库与工程化配置都只是周边。点击右侧圆点可标记学习状态。

<CategoryNotesIsland categoryId="react/basic/core" />
