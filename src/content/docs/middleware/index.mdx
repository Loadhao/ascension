---
title: 中间件
description: 中间件横向综述：为什么需要消息队列、主流 MQ 选型对比、消息可靠性三问
---

import RoadmapIsland from '../../../components/learn/RoadmapIsland.astro';
import KnowledgeGraph from '../../../components/KnowledgeGraph';
import graph from '../../../data/graphs/middleware.json';

## 学习路线

按等级推进，每级下按主题分类。点击分类进入该主题的知识点目录；点击圆点标记学习状态：
○ 未学 → ◐ 学习中 → ● 已掌握。

本方向收录**跨工具横向内容**：为什么需要消息队列（解耦/异步/削峰与代价）、
三大主流 MQ 选型（Kafka / RocketMQ / RabbitMQ），以及中级专题
**消息可靠性三问**（不丢、不重、不乱序的通用解法）。各具体中间件的
深入笔记见独立的 **Kafka** 与 **RocketMQ** 方向。后续可扩展
ZooKeeper / etcd 等横向综述。

<RoadmapIsland directionId="middleware" />

## 知识图谱

<KnowledgeGraph client:visible data={graph} />

:::note[图谱说明]
节点可拖拽、滚轮/双指缩放，右上角按钮可缩放、复位视图；悬停或点按节点可高亮关联关系；点击圆点进入对应笔记，左下图例点按可按分组筛选。
图谱数据维护在 `src/data/graphs/middleware.json`。
:::
