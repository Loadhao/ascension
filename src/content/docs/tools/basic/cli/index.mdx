---
title: 命令行工具
description: grep/sed/awk 文本处理、curl 调试接口、jq 处理 JSON——高频终端工具速查
level: basic
---

import CategoryNotesIsland from '../../../../../components/learn/CategoryNotesIsland.astro';

把「这几天常碰、但总是搜」的命令行工具收进一处：文本处理（grep/sed/awk）、
接口调试（curl）、JSON 处理（jq）。要点是**记住核心思路而不是背选项**，
具体参数用时现查。点击右侧圆点可标记学习状态（未学 → 学习中 → 已掌握）。

<CategoryNotesIsland categoryId="tools/basic/cli" />