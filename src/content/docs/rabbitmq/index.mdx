---
title: RabbitMQ
description: RabbitMQ 方向学习路线与知识地图：AMQP 模型、交换机与绑定、可靠投递、死信与延迟
---

import RoadmapIsland from '../../../components/learn/RoadmapIsland.astro';
import KnowledgeGraph from '../../../components/KnowledgeGraph';
import graph from '../../../data/graphs/rabbitmq.json';

## 学习路线

按等级推进，每级下按主题分类。点击分类进入该主题的知识点目录；点击圆点标记学习状态：
○ 未学 → ◐ 学习中 → ● 已掌握。

当前覆盖 **AMQP 模型**（交换机、队列与绑定、消息确认）→ **可靠与进阶**
（ACK/重试、死信与延迟队列、集群与高可用），后续可扩展插件生态（延迟交换机、
一致性哈希）与性能调优等主题。

<RoadmapIsland directionId="rabbitmq" />

## 知识图谱

<KnowledgeGraph client:visible data={graph} />

:::note[图谱说明]
节点可拖拽、滚轮/双指缩放，右上角按钮可缩放、复位视图；悬停或点按节点可高亮关联；加粗节点已关联笔记、点击跳转，左下图例点按可按分组筛选。
图谱数据维护在 `src/data/graphs/rabbitmq.json`。
:::