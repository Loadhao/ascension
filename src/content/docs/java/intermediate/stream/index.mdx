---
title: Stream 与函数式
description: Stream 管道原理、Spliterator 拆分、ForkJoinPool 工作窃取、Collector 五件套与并行归约的正确性契约
---

import CategoryNotesIsland from '../../../../../components/learn/CategoryNotesIsland.astro';

声明式数据处理是 Java 8 的分水岭。先看管道与延迟执行的接口骨架，
再看 parallelStream 背后的 ForkJoinPool——并行的收益边界
（NQ 模型、数据源可分割性）比用法本身更值得掌握。第二篇收在终点那一步：
`collect` 收到的 Collector 承诺了什么（combiner 契约、三个特征值、
`toMap` 的两种异常），那里才是"本地对、线上偶尔错"的发生地。
点击右侧圆点可标记学习状态（未学 → 学习中 → 已掌握）。

<CategoryNotesIsland categoryId="java/intermediate/stream" />
